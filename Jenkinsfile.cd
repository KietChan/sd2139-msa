pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                // Your deployment steps here
                echo 'Deploying application...'
                // e.g., sh 'deploy.sh'
            }
        }
    }
}
